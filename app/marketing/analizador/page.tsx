import { AnalizadorContent } from "@/components/marketing/analizador/AnalizadorContent"
export const metadata = { title: "Analizador de Reels" }
export default function AnalizadorPage() {
  return <AnalizadorContent />
}
